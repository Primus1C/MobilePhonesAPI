&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	//ПоТовару = Справочники.Номенклатура.ПустаяСсылка();
	//ПоПартии = Справочники.Партии.ПустаяСсылка();
	//Если Параметры.Свойство("Отбор") Тогда
	//	Если Параметры.Отбор.Свойство("Партия") Тогда
	//		ПоПартии = Параметры.Отбор.Партия;
	//		ПоТовару = ПоПартии.Владелец;
	//		Элементы.ПоТовару.Видимость = Истина;
	//		Элементы.ПоПартии.Видимость = Истина;
	//	КонецЕсли;
	//КонецЕсли;
КонецПроцедуры
