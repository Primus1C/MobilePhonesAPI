&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	//ПоТовару = Справочники.Номенклатура.ПустаяСсылка();
	//Если Параметры.Свойство("Отбор") Тогда
	//	Если Параметры.Отбор.Свойство("Номенклатура") Тогда ПоТовару = Параметры.Отбор.Номенклатура КонецЕсли;
	//КонецЕсли;
	//Элементы.ПоТовару.Видимость = ЗначениеЗаполнено(ПоТовару);
КонецПроцедуры
