
Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)
	
	// 1. Отключаем стандартный алгоритм, так как нам нужно вмешаться в результат
	СтандартнаяОбработка = Ложь;
	
	// 2. Стандартный программный вывод отчета (формируем его "под капотом")
	Настройки = КомпоновщикНастроек.ПолучитьНастройки();
	КомпоновщикМакета = Новый КомпоновщикМакетаКомпоновкиДанных;
	Макет = КомпоновщикМакета.Выполнить(СхемаКомпоновкиДанных, Настройки, ДанныеРасшифровки);
	
	ПроцессорКомпоновки = Новый ПроцессорКомпоновкиДанных;
	ПроцессорКомпоновки.Инициализировать(Макет, , ДанныеРасшифровки);
	
	ПроцессорВывода = Новый ПроцессорВыводаРезультатаКомпоновкиДанныхВТабличныйДокумент;
	ПроцессорВывода.УстановитьДокумент(ДокументРезультат);
	ПроцессорВывода.Вывести(ПроцессорКомпоновки);
	
	// 3. САМОЕ ГЛАВНОЕ: Сворачиваем физически сформированный отчет
	// Уровень 0 - это Общие итоги
	// Уровень 1 - это первая группировка (например, Контрагент)
	// Уровень 2 - это вторая группировка (например, Инвойс)
	ДокументРезультат.ПоказатьУровеньГруппировокСтрок(0);
	
КонецПроцедуры
